title: Video and Talk

icon: 🖼

options:
  preview: false

status:
  draft: true
  listed: true

columns:
  - width: 1/3
    fields:
      vimeoLink:
        type: url
        label: Lien Vimeo
      category:
        label: Category
        type: multiselect
        options:
          type: api
          url: https://raw.githubusercontent.com/azertypow/jeansding.backend/main/categories.json
          query: categories
          text: "{{ item.name }}"
          value: "{{ item.name.slug }}"
